machine:
  php:
    version: 7.0.11

dependencies:
  override:
    - npm install -g yarn
    - npm install -g gulp
    - yarn
    - cp .env.test .env
    - gulp
  cache_directories:
    - vendor

deployment:
  production:
    branch: master
    commands:
      - vendor/bin/phing bundle

  staging:
    branch: develop
    commands:
      - vendor/bin/phing bundle

general:
  artifacts:
    - "storage/logs"
    - "armabyte.zip"
